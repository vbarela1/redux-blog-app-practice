import React from 'react';
import { connect } from 'react-redux';


const Blog = ({ id, title, dispatch }) => (

   <li>{ title }</li>
  //build a toggle edit
  //


);

export default connect()(Blog);
